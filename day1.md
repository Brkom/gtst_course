#DAY1: ETHICAL HACKING

1.cd desktop
to open desktop directory 2.mkdir "new folder" && cd your "saved folder"
mkdir to create a folder 3.git init
it will initialize 4.git conifig --global user.name "your github user name"
it is used to enter or link ur github acc 5.git conifig --global user.email "your email"
it is used to enter or link ur email acc
FORNORMALWORKS
1.git status
it is used to track files we didn't add before we commit 2.git add .
it is used to add files before we commit 3.git commit -m "your commit"
it is used to commit file after we add 4.git log
it is used to know commited info
for github
1.git remote add origin "repositoryURL"
it is used to update our acc to new repo... 2.git push -u origin master
it is used to push our file to our repo 3.git clone "your project link"
it is used to download some projects
